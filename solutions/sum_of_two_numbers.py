"""Solution for the sum_of_two_numbers problem."""


def solve(input_data):
    """
    Calculate the sum of two numbers.
    
    Args:
        input_data: A dictionary with keys 'a' and 'b' containing numbers to sum.
        
    Returns:
        The sum of the two numbers.
    """
    return input_data["a"] + input_data["b"]
