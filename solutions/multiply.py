"""Buggy solution for the multiply problem (for testing)."""


def solve(input_data):
    """
    Intentionally buggy multiply function to demonstrate error handling.
    """
    # Bug: This uses addition instead of multiplication
    return input_data["a"] + input_data["b"]
